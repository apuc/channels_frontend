import getters from './getters';
import actions from './actions';
import mutations from './mutations';

const state = {
  integrations_types:[],
  integrationsList: [],
};

export default {
  state,
  getters,
  actions,
  mutations,
};
